c       implicit real*8(a-z)
        implicit none
        real a,b,s
         print*,'a,b'
         read*,a,b
         s=a+b
         print 5,s
5       format('sum=',f10.2)
        read *,
         end
