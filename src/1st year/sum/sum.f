!     Sum of two numbers.
      implicit none
      integer :: a,b,sum
      print*, 'enter two number'
      read*, a,b
      sum = a + b
      print*, 'sum of the given numbers is', sum
      read*,
      end
